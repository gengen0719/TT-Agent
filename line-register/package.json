{
  "name": "tt-line-register",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "LINE_CHANNEL_SECRET=test LINE_CHANNEL_ACCESS_TOKEN=test LINE_USERS_TABLE_NAME=LINE_Users node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "~5.9.0"
  }
}
